/**
 * @lexii/ai — AI 能力包（空壳）
 *
 * 本包在 MVP 阶段**故意为空**：Lexii 是 local-first 应用，
 * MVP 不引入任何必须联网或依赖外部 AI 服务的能力（详见 README）。
 * 所有 AI 相关代码只允许出现在本包内，禁止散落到其他包或 apps/。
 */

/** 包名 */
export const PACKAGE_NAME = "@lexii/ai";
